<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{pageTitle}}</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.15/dist/tailwind.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Courier New', monospace;
            background: #87CEEB;
            color: #000000;
        }
        .pixel-border {
            border: 2px solid #000000;
            margin-top: 20px;
        }
        .gradient-bar {
            background: linear-gradient(90deg, #FF00FF 0%, #FF4500 33%, #FFD700 66%, #FF00FF 100%);
            height: 60px;
            width: 100%;
        }

         #ai-response p {
        margin-bottom: 30px; /* Adjust the value as per your needs */
    }
    </style>
</head>
<body class="bg-light-blue text-black">
<div id="registration-modal" class="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black bg-opacity-50 hidden">
  <div class="bg-white rounded-lg p-8 relative">
    <span class="absolute top-2 right-2 cursor-pointer close">
      &times;
    </span>
    <p class="text-xl font-semibold">Welcome! You have successfully registered.</p>
  </div>
</div>


 <button id="logoutButton" class="bg-light-blue text-black border-2 border-black p-2 rounded font-mono absolute top-2 right-2">Logout</button>
    <div class="gradient-bar"></div>
    <div class="container mx-auto flex">
        <div id="history" class="w-3/12 p-6 pixel-border">
            <h1 class="text-xl font-mono mb-4">Wellness History</h1>
          <ul id="history-list">
    {{#each history}}
        <li data-date="{{this.date}}" class="mb-2 cursor-pointer transition hover:bg-gray-200">
            <button class="text-base font-mono w-full text-left p-2" onclick="onConversationClick({{this.id}})">
                {{this.date}}
            </button>
        </li>
    {{/each}}
</ul>
        </div>
        <div id="interaction" class="w-9/12 ml-6 p-6 pixel-border">
            <h1 class="text-xl font-mono mb-4">Wellness Session</h1>
            <h1 class="text-base font-mono mb-4">Let the wellness chatbot know how you're feeling to receive support</h1>
            <form id="wellness-form" class="flex items-end">
                <textarea id="query" placeholder="How are you feeling today?" class="flex-grow p-2 rounded font-mono pixel-border h-12"></textarea>  <!-- Black border -->
                <button type="submit" class="bg-light-blue text-black border-2 border-black p-2 rounded font-mono ml-4 h-12">Log</button>  <!-- Black text and border -->
            </form>
            <div id="ai-response" class="mt-4">
                <p class="text-base font-mono"></p>
            </div>
        </div>
    </div>
    <script src="/public/js/chatPage.js" ></script>
</body>
</html>
